type TProfileInfo = {
  name: string;
  avatar: string;
  role: string;
  about: string;
  availability: string;
};

type TContacts = {
  icon: string;
  text: string;
};

export type { TProfileInfo, TContacts };
